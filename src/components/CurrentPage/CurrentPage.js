import "./CurrentPage.css";

const CurrentPage = ({ children }) => {
  return (
    <div className="current-page">
      <div className="current-page__text"> Home </div>
    </div>
  );
};

export default CurrentPage;
